/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "img.icons8.com",
      "images.unsplash.com",
      "via.placeholder.com",
      "picsum.photos",
    ],
  },
};

export default nextConfig;
